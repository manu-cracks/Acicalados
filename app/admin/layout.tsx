import '../globals.css'

export const metadata = {
  title: 'Admin — ACICALADOS',
  description: 'Panel administrativo de ACICALADOS',
}

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
